<section class="about" id="ueber-mich">
  <div class="media">
    <video
      poster="/uploads/video-poster.jpg"
      autoplay
      loop
      muted
      playsinline
      aria-label="Lorenz Beglinger bei einem Trick, stumme Endlosschleife"
    >
      <source src="/uploads/lorenz-video.mp4" type="video/mp4" />
    </video>
    <div class="media-scrim" aria-hidden="true"></div>
    <div class="media-tag">
      <span class="dot" aria-hidden="true"></span>
      Video · Endlosschleife, ohne Ton
    </div>
  </div>

  <div class="copy">
    <p class="kicker" style="color:var(--gold-deep)">Über mich</p>
    <h2>
      Ich heisse Lorenz Beglinger und zaubere seit ich zehn Jahre alt bin.
    </h2>
    <p>
      Ich trete mit meinem Programm vor allem an Kindergeburtstagen und
      Familienfesten in der ganzen Nordwestschweiz auf.
    </p>
    <p>
      Was ich beim zaubern bis heute am meisten mag ist der Moment kurz nach
      dem Trick. Wenn das Publikum lacht, weil es nicht weiss, was gerade
      geschehen ist.
    </p>
  </div>
</section>

<style>
  .about {
    display: grid;
    grid-template-columns: 1fr 1fr;
    background: var(--bg-cream);
  }

  .media {
    position: relative;
    min-height: 620px;
    background: #d9d3c6;
    overflow: hidden;
  }

  .media video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 50% 28%;
  }

  .media-scrim {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 180px;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0) 0%,
      rgba(0, 0, 0, 0.45) 100%
    );
  }

  .media-tag {
    position: absolute;
    left: 26px;
    bottom: 24px;
    display: flex;
    align-items: center;
    gap: 10px;
    font: 400 11px/1 var(--font-sans);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.85);
  }

  .dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--gold);
  }

  .copy {
    padding: clamp(56px, 8vw, 96px) clamp(28px, 6vw, 70px);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .copy h2 {
    margin: 0 0 28px;
    font: 300 clamp(1.9rem, 1.3rem + 2vw, 2.875rem) / 1.14 var(--font-serif);
    color: var(--text-dark);
  }

  .copy p {
    margin: 0 0 20px;
    font: 300 17px/1.8 var(--font-sans);
    color: var(--text-dark-dim);
  }

  .copy p:last-child {
    margin-bottom: 0;
  }

  @media (max-width: 900px) {
    .about {
      grid-template-columns: 1fr;
    }

    .media {
      min-height: 320px;
    }
  }
</style>
