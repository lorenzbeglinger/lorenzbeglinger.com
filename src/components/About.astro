<section class="about" id="ueber-mich">
  <div class="media">
    <video
      poster="/uploads/video-poster.jpg"
      autoplay
      loop
      muted
      playsinline
      aria-label="Lorenz Beglinger bei einem Trick, stumme Endlosschleife"
    >
      <source src="/uploads/lorenz-video.mp4" type="video/mp4" />
    </video>
    <div class="media-scrim" aria-hidden="true"></div>
  </div>

  <div class="copy">
    <p class="kicker">Über mich</p>
    <h2>Ich heisse Lorenz Beglinger und bin 12 Jahre alt.</h2>
    <p>
      Ich zaubere seit ich zehn Jahre alt bin leidenschaftliche gerne. Was ich
      dabei bis heute am meisten mag ist der Moment kurz nach dem Trick. Wenn
      das Publikum staunt, weil es nicht weiss, was gerade geschehen ist.
    </p>
  </div>
</section>

<style>
  .about {
    display: grid;
    grid-template-columns: 1fr 1fr;
    background: var(--bg-cream);
  }

  .media {
    position: relative;
    min-height: 620px;
    background: #d9d3c6;
    overflow: hidden;
  }

  .media video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 50% 76%;
  }

  .media-scrim {
    position: absolute;
    left: 0;
    right: 0;
    bottom: 0;
    height: 180px;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0) 0%,
      rgba(0, 0, 0, 0.45) 100%
    );
  }

  .copy {
    padding: clamp(56px, 8vw, 96px) clamp(28px, 6vw, 70px);
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .copy h2 {
    margin: 0 0 28px;
    font: 300 clamp(1.9rem, 1.3rem + 2vw, 2.875rem) / 1.14 var(--font-serif);
    color: var(--text-dark);
  }

  .copy p:not(.kicker) {
    margin: 0 0 20px;
    font: 300 17px/1.8 var(--font-sans);
    color: var(--text-dark-dim);
  }

  .copy p:last-child {
    margin-bottom: 0;
  }

  @media (max-width: 900px) {
    .about {
      grid-template-columns: 1fr;
    }

    .media {
      min-height: 450px;
    }

    .media video {
      object-position: 50% 78%;
    }
  }
</style>
