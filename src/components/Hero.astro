<section class="hero" id="top">
  <div class="hero-media">
    <img
      src="/uploads/LB-Hero.jpg"
      alt="Lorenz Beglinger bei einem Kartentrick"
      class="hero-photo"
      fetchpriority="high"
    />
    <div class="hero-scrim-x" aria-hidden="true"></div>
    <div class="hero-scrim-y" aria-hidden="true"></div>
  </div>

  <div class="hero-copy container">
    <p class="kicker">Kinder Zauberkunst</p>
    <h1>
      Wunder passieren.<br />
      <em>Direkt vor deinen Augen.</em>
    </h1>
    <p class="lead">
      Ich heisse Lorenz Beglinger und bringe ein wenig Magie an dein
      Geburtstagsfest oder deine Familienfeier. Keine Bühne nötig — nur ein
      Wohnzimmer und ein paar Gäste, die kurz vergessen wollen wie die Welt
      funktioniert.
    </p>
    <div class="hero-actions">
      <a href="#anfrage" class="btn">Jetzt anfragen</a>
      <a href="#ueber-mich" class="hero-link">
        Über mich<span aria-hidden="true">↓</span>
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 660px;
    display: flex;
    align-items: flex-end;
    padding-top: 100px;
    padding-bottom: 64px;
    background: radial-gradient(
      120% 95% at 78% 34%,
      #5c5c60 0%,
      #3a3a3f 34%,
      #1d1d23 66%,
      #0b0b0e 100%
    );
    overflow: hidden;
  }

  .hero-media {
    position: absolute;
    inset: 0;
  }

  .hero-photo {
    position: absolute;
    top: 0;
    right: 0;
    height: 100%;
    width: 66%;
    object-fit: cover;
    object-position: 52% 8%;
    filter: saturate(0.9) contrast(1.02);
    -webkit-mask-image: linear-gradient(
      100deg,
      transparent 0%,
      rgba(0, 0, 0, 0.35) 16%,
      #000 42%
    );
    mask-image: linear-gradient(
      100deg,
      transparent 0%,
      rgba(0, 0, 0, 0.35) 16%,
      #000 42%
    );
  }

  .hero-scrim-x {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      98deg,
      rgba(11, 11, 14, 0.94) 0%,
      rgba(11, 11, 14, 0.8) 32%,
      rgba(11, 11, 14, 0.42) 52%,
      rgba(11, 11, 14, 0.05) 72%,
      rgba(11, 11, 14, 0.3) 100%
    );
  }

  .hero-scrim-y {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(11, 11, 14, 0.55) 0%,
      rgba(11, 11, 14, 0) 26%,
      rgba(11, 11, 14, 0) 55%,
      rgba(11, 11, 14, 0.86) 88%,
      #0b0b0e 100%
    );
  }

  .hero-copy {
    position: relative;
    max-width: 760px;
    margin: 0;
  }

  h1 {
    margin: 0;
    font: 300 clamp(2.75rem, 3.2rem + 2.2vw, 4.625rem) / 1.06 var(--font-serif);
    color: var(--text-cream);
    letter-spacing: -0.01em;
    text-wrap: balance;
  }

  h1 em {
    font-style: italic;
    color: var(--gold);
  }

  .lead {
    margin: clamp(20px, 3vw, 30px) 0 0;
    max-width: 520px;
    font: 300 clamp(1rem, 0.85vw + 0.85rem, 1.1875rem) / 1.7 var(--font-sans);
    color: var(--text-cream-dim);
  }

  .hero-actions {
    display: flex;
    gap: 28px;
    margin-top: clamp(30px, 4vw, 44px);
    align-items: center;
    flex-wrap: wrap;
  }

  .hero-link {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    color: var(--text-cream-dim);
    font: 400 15px/1 var(--font-sans);
    letter-spacing: 0.06em;
    border-bottom: 1px solid var(--border-gold-strong);
    padding-bottom: 7px;
    transition: color 0.15s ease, border-color 0.15s ease;
  }

  .hero-link:hover {
    color: var(--gold);
    border-bottom-color: var(--gold);
  }

  .hero-link span {
    font-size: 16px;
    line-height: 1;
  }

  @media (max-width: 900px) {
    .hero {
      min-height: 100svh;
      padding-top: 90px;
      padding-bottom: 44px;
      align-items: flex-end;
    }

    .hero-photo {
      width: 100%;
      object-position: 62% 12%;
      mask-image: none;
      -webkit-mask-image: none;
    }

    .hero-scrim-x {
      background: linear-gradient(
        180deg,
        rgba(11, 11, 14, 0.75) 0%,
        rgba(11, 11, 14, 0.25) 40%,
        rgba(11, 11, 14, 0.55) 70%,
        rgba(11, 11, 14, 0.97) 100%
      );
    }

    .hero-scrim-y {
      background: none;
    }

    .hero-copy {
      max-width: 100%;
    }

    .hero-actions .btn {
      width: 100%;
    }
  }
</style>
